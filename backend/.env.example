# SECURITY WARNING: keep the secret key used in production secret!
# Generate a new key using: openssl rand -hex 32
SECRET_KEY=change_this_to_a_very_secure_random_string

# JWT Algorithm (default is HS256)
ALGORITHM=HS256

# Access Token Expiration (minutes)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS Configuration
# Comma-separated list of allowed origins (e.g. https://yourdomain.com,https://admin.yourdomain.com)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Database Configuration
# For Production, use PostgreSQL: postgresql://user:password@localhost/dbname
DATABASE_URL=sqlite:///./sql_app.db

# Frontend URL (for email links, etc.)
FRONTEND_URL=http://localhost:3000
